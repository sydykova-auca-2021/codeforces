#include <bits/stdc++.h>

//typedef long long ll;
using ll = long long;

void solve() {
    int n;
    std::vector<int> a(n);

    for(int i = 0; i < n; ++i){
        std::cin >> a[i];
    }


}


int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);

    solve();
    return 0;
}
