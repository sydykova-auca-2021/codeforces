#include <bits/stdc++.h>

void solve(){
    int a;
    std::cin >> a;


    for(int i = 1; i < a; ++i){
        if(a % i == 0){

        }
    }
}

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);

    solve();
    return 0;
}
