#include <bits/stdc++.h>

void solve(){
    int n,l,a;
    std::cin >> n >> l >> a;

    // cust, min,
}

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);

    solve();
    return 0;
}
