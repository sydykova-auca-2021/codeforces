#include <bits/stdc++.h>

void solve(){
    int t;
    std::cin >> t;

    std::string target = "Timur";

    while(t--) {
        int n;
        std::cin >> n;
        std::string str;


        for(int i = 0; i < n; ++i){
            std::cin >> str;
        }
    }
}

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);

    solve();
    return 0;
}
