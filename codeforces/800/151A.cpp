#include <bits/stdc++.h>

// nl(drink) + lime + np(salt)

// n = friends

// k = bottles of drink
// l = ml of drink

// c = limes
// d = slices of lime

// p = grams of salt

// nl
// np

void solve(){
    int n,k,l,c,d,p,nl,np;
    std::cin >> n >> k >> l >> c >> d >> p >> nl >> np;




}

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);

    solve();
    return 0;
}
