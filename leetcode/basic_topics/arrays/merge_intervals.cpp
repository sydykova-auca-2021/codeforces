#include <bits/stdc++.h>

class Solution {
public:
    std::vector<std::vector<int>> merge(std::vector<std::vector<int>>& intervals) {
        // if b.first < a.second

        for(int i = 0; i < intervals.size(); ++i){
            if(intervals[i+1].first intervals[i+1]){

            }
        }
    }
};