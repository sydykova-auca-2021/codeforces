#include <bits/stdc++.h>

// sliding window
// two pointers

class Solution {
public:
    int characterReplacement(std::string s, int k) {
        // AABABBA
        // 1
        // 4

        int left = 0;
        int right = 0;
        int max_count = 0;


    }
};