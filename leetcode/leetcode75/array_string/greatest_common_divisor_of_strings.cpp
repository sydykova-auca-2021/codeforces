#include <bits/stdc++.h>

class Solution {
public:
    std::string gcdOfStrings(std::string str1, std::string str2) {

        int i = 0, j = 0;

        while(i < str1.length() && j < str2.length()){

        }

    }
};

int main(){
    Solution solution;
    std::string word1 = "ab", word2 = "pqrs";
    std::cout << "Merged string: " << solution.gcdOfStrings(word1, word2) << "\n";
    return 0;
}